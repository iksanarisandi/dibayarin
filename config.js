/**
 * Konfigurasi global dibayar.in
 * Edit file ini untuk mengubah markup, fee, dan nomor WhatsApp
 */

const CONFIG = {
    // Markup kurs per USD
    MARKUP: 1100,

    // Fee tetap per transaksi
    FEE: 50000,

    // Nomor WhatsApp (tanpa + atau 0 di depan)
    WA_NUMBER: '6282347303153'
};
